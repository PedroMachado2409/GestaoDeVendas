# Endereço da API Gestão de Pedidos.
# O perfil "http" do backend (dotnet run) escuta em http://localhost:5143.
# Se rodar o perfil "https", troque para https://localhost:7086 e confie no
# certificado de desenvolvimento (dotnet dev-certs https --trust).
VITE_API_URL=http://localhost:5143
